{
  "frontend/src/components/FeaturedProductsSlider.tsx": "Horizontal scrollable slider showcasing 4 featured rice varieties with scroll buttons (desktop only via a wrapper div) and \"View Details\" links, fixing the conflicting Tailwind class lint error.",
  "frontend/src/pages/Contact.tsx": "Public inquiry form with all 8 required fields (Name, Company Name, Country, Category of Rice, Quantity MT, Email, Phone, and optional Message), full validation, loading spinner during submission, inline error messages, and a success confirmation screen with reset option.",
  "frontend/src/components/Footer.tsx": "Site-wide footer with AZAD NEXUS logo, quick links, contact details, and a new social links section featuring LinkedIn and Instagram brand icons (gold-tinted) linking to the respective profiles, plus copyright and caffeine.ai attribution.",
  "frontend/src/data/products.ts": "Product data file defining the `Product` interface (now with optional `imagePath`) and the array of 6 rice varieties, each mapped to its corresponding generated image filename in `/assets/generated/`.",
  "frontend/src/components/ProductCard.tsx": "Product card component that now renders a 4:3 aspect-ratio product image above the royal-blue header when `imagePath` is provided, with a subtle hover zoom and gradient overlay, followed by the existing specs table.",
  "frontend/src/components/Navigation.tsx": "Sticky top navigation bar with the AZAD NEXUS logo using `mix-blend-mode: screen` and a gold amber `drop-shadow` filter to eliminate the white background clash against the deep royal blue nav, plus desktop nav links, a \"Request a Quote\" CTA, and a responsive mobile menu.",
  "frontend/src/components/WhatsAppButton.tsx": "Sticky floating action button fixed at the bottom-right corner of every page, using the WhatsApp brand icon and green color, linking to wa.me/917058779219 for instant tap-to-chat on all devices.",
  "frontend/src/App.tsx": "Main app component with TanStack Router setup, Layout shell, and all page routes including the new `/admin/login` route rendering `AdminLogin`.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for all backend operations — `useSubmitInquiry` (public), `useValidateAdmin` (calls backend `validateAdmin` mutation), `useGetInquiries`/`useMarkResolved`/`useDeleteInquiry` (all read the access key from sessionStorage and pass it to the backend), and `useIsCallerAdmin` (kept for compatibility).",
  "frontend/src/pages/Admin.tsx": "Admin dashboard that guards access via local sessionStorage session (redirects to `/admin/login` if unauthenticated), displays inquiry stats and a full sortable table with view, mark-resolved, and delete actions — no Internet Identity dependency.",
  "frontend/src/components/InquiriesTable.tsx": "Stub component kept for import compatibility; the full inquiry table is now embedded directly in Admin.tsx.",
  "frontend/src/hooks/useAuth.ts": "Simple sessionStorage-based admin session manager that stores and retrieves the backend-issued access key under `adminAccessKey`, with helpers for `isAdminAuthenticated`, `saveAccessKey`, `getAccessKey`, and `logout`.",
  "frontend/src/pages/AdminLogin.tsx": "Admin login page that calls the backend `validateAdmin` function via the `useValidateAdmin` mutation hook; on success saves the returned access key to sessionStorage and navigates to `/admin`, on failure shows an inline error — no Internet Identity dependency."
}